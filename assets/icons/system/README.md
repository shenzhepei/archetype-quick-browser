# System icons

These SVG paths were extracted from the generated `@e-cloud/eslink-icons` system icon sources.
Only the SVG markup is used; the Vue and TypeScript runtime is not included.

Source package license: ISC.
